{% extends 'base.html.twig' %}

{% block title %}
    Form image
{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn btn-light">
        Ajouter
    </button>
    {{ form_end(form) }}

{% endblock body %}
