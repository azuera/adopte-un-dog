{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}


{% block body %}
    <div class="container">
    <h2 class="mb-2">Créer une nouvelle annonce</h2>
        {{ form_start(form) }}
            {# {{ form_row(form.title) }} #}
        {{ form_end(form) }}
    </div>

{% endblock body %}